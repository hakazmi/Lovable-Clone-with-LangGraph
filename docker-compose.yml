services:
  lovable-lite:
    build:
      context: .
    ports:
      - "8081:8081"
      - "3000-3050:3000-3050"
    volumes:
      - .:/app
      - /app/work
    env_file:
      - .env
    restart: unless-stopped

